{
  "name": "subgraph-dao-delegates-polygon",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service show-karma/dao-delegates-polygon",
    "create-local": "graph create --node http://localhost:8020/ arthh/subgraph-dao-delegates-polygon",
    "remove-local": "graph remove --node http://localhost:8020/ arthh/subgraph-dao-delegates-polygon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 arthh/subgraph-dao-delegates-polygon"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
